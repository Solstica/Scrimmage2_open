# 建模手完成记录 15：Q2/Q3 伪代码与模型求解正式化

## Q2

已在 `feature/q2-paper-a` 完成：

- 新增 `modules/30_q2/paper/q2_algorithm.tex`，统一为 Q1 的 `algorithm2e` 标准样式；
- 删除正文对旧 `q2_pseudocode.txt` 的 `lstinputlisting` 调用；
- 明确两阶段多初值求解：90 组阶段一粗搜索、候选去重、前 8 个不同候选完整十参数精修、最低 RMSE 候选判别；
- 明确 `(round(d,3), round(q_N,2))` 仅对应数值候选压缩分辨率，不赋予物理盆地边界含义；
- 增加主要计算量 `O[(90L1+8L2)m]` 的说明；
- 两个角度始终独立反演，最终仅对厚度作算术平均。

## Q3

已在 `feature/q3-paper-a` 完成：

- 新增 `modules/40_q3/paper/q3_algorithm.tex`，统一为 Q1/Q2 的 `algorithm2e` 标准样式；
- 删除正文对旧 `q3_pseudocode.txt` 的 `lstinputlisting` 调用；
- 显式登记 6 组四参数确定性初值；
- 明确每个初值均使用同一个正式 Airy 模型和相同物理边界；
- 边界活跃参数仅记录为可辨识性诊断，不自动丢弃；
- 将同参双光束比较、Jacobian--SVD 与 SiC 第三束强度比回溯明确放在正式 Airy 候选确定之后，不参与重新拟合或模型切换；
- 增加两角度六初值反演主要计算量 `O(12Lm)` 的说明。

## 公共样式

Q2、Q3 分支的 `paper/preamble.tex` 已同步当前 `feature/paper-common-final` canonical 版本，以支持 `algorithm2e`、`setspace`、`placeins` 并避免旧 preview preamble 重复加载 geometry。

本轮不改变冻结结果：Q2 = 7.7398 μm；Q3 Si = 3.2178 μm；Q3 回溯 SiC = 7.7398 μm。
