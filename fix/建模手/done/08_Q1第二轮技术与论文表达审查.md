# Q1 第二轮技术与论文表达审查（已完成）

对应正文：`feature/q1update/modules/20_q1/paper/q1.tex`。

## 已完成修正

- [x] 删除原先不严谨的 `d -> 0` 双光束零厚度自洽性检验；明确该严格极限属于完整多光束级数，不作为截断双光束模型验证条件。
- [x] 增加一般 Drude 复介电响应
  `epsilon_tilde = epsilon_L - omega_p^2/[omega(omega+i Gamma)]`，再推到 Q1/Q2 正式使用的弱吸收实部近似。
- [x] 二级标题由方法名堆叠改为简洁的 `模型建立`，三个三级标题分别承担光程差、折射率和 Fresnel 双光束模型。
- [x] 问题分析段删除“带符号 Fresnel/不加半波损失”等过早的技术结论，只保留任务、关键关系和总体路线。
- [x] 增加 `bornwolf`、`drude`、`paperA` 正式引用；`m^*=0.28m0` 明确为消除 `N/m*` 结构冗余并与当前数值路线一致的固定设置，不将其误写成由本题数据独立识别所得。
- [x] 明确空气折射率数值口径 `n1=1.0003`，与 Q2 正式程序 `AIR_INDEX=1.0003` 一致。
- [x] 明确 Sellmeier 参数 `C_j` 的单位为 `um^2`，避免其与 `lambda^2-C_j` 的量纲冲突。
- [x] 将模型自洽性检验改为四项可直接复核的小表：零载流子极限、正入射 Fresnel 极限、相位对厚度线性、带符号 Fresnel 相位处理。

## 保持不变的冻结口径

- Q1/Q2 的 SiC 正式路线仍采用弱吸收实折射率近似；
- 带符号 Fresnel 振幅系数承担界面反射相位，不额外固定增加 `pi`；
- Q1 仍是表面反射光 + 第一束内部反射光的双光束截断模型；
- Q2 正式数值和 Q3 正式数值均未因本轮文字修正改变。

## 仍由其他角色/章节追踪

- Q1 流程图目前仍是旧版占位图，内部 `PAPER_A`、`合并处理模块`、旧 Sellmeier/Drude 公式和串行依赖关系需按 `fix/论文手/todo/03_流程图更新清单.md`、`06_占位图先插入与Q1图文回填.md` 最终替换。
- 全局符号表当前只列公共符号，`A,B_j,C_j,N,m^*,Gamma` 仍按“首次出现处定义”的原则处理；如后续论文手扩充全局符号表，应保持 `C_j` 单位为 `um^2`。
