# AI 使用报告续作指南

唯一正文源：
```text
modules/80_ai_report/paper/ai_report.tex
```

canonical branch：
```text
feature/paper-common-final
```

初始化：
```bash
git fetch origin --prune
git switch -c task/paper/ai-report-<topic>-YYYYMMDD origin/feature/paper-common-final
```

要求：
- 如实描述 AI 辅助范围；
- 不声称 AI 独立完成模型选择或数值验证；
- 保持与实际协作流程一致；
- 不在模块内再加一个重复的 `\section*{AI使用报告}`，标题由全文结构负责。
