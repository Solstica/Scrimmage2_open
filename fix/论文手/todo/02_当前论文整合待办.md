# 当前论文整合待办

## 已确认保留
- 最终 PAPER_A 的 Q1/Q2/Q3 模型路线与冻结结果；
- 队友在旧 `modules/paper汇总/document.tex` 中做出的有价值版式设计，按“公共格式补丁”迁移，不迁移全文正文；
- Origin/Excel/PNG 可编辑图表资产，即使目前数据来自旧路线，也先保留后更新。

## 字体/版式
旧 `feature/paper` 的 `modules/paper汇总/document.tex` 已采用：
- 中文 `fontset=fandol`；
- 英文 Times New Roman；
- 章节标题使用宋体类命令，但在 Fandol fontset 下实际映射到 Fandol 字体族；
- 2.5 cm 页边距、1.38 行距等。

正式模块化论文需要把“字体/公共版式”迁入 `paper-common-final`，而不是继续维护旧 `document.tex`。

注意：`80a2123` 中所谓“摘要宽度与正文一致”的环境重定义并不是字体补丁；当前正式 class 的 quotation 左右 margin 已经是 0。该段还会改变摘要标题和分页行为，因此不要整段搬入。

## 图表
Q2/Q3 老 Origin 图不删除。未完成数据更新前，正文中不得用这些图证明最终数值。
对应建模与编程任务见：
- `fix/建模手/todo/01_Origin图表模型口径更新.md`
- `fix/编程手/todo/01_Origin图表数据重算与导出.md`
