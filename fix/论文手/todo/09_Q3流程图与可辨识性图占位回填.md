# Q3 流程图与可辨识性图占位回填

## 1. 总体流程图

当前正文已插入：

`modules/40_q3/figures/editable/origin_templates/reference_png/q3_flow_reference.png`

作为版面占位。该图仍含旧版“η3 阈值决定双光束/Airy”的错误逻辑，最终必须改为：

附件3、4与预处理
→ 双振子--Drude复折射率
→ 实角Snell + 复Fresnel/传播
→ Airy无限多光束正式正演
→ 10°、15°四参数独立拟合
→ 两厚度算术平均 3.2178 μm
→ Jacobian--SVD可辨识性验证
→ SiC第三束光强比回溯
→ 保留7.7398 μm。

禁止出现：

- `η3 >= 0.1% ?` 选模菱形；
- “满足阈值才启用Airy”；
- 正式双光束分支与Airy分支二选一。

η3 只能作为旁路解释/SiC回溯量。

## 2. 参数可辨识性图

当前正文临时使用旧：

`19_Si参数可辨识性气泡图.png`

最终应从 common-final 最新数据：

`modules/40_q3/figures/editable/origin_data/csv/q3_identifiability_parameters.csv`

重绘。推荐口径：

- X：d, n3, log10N, log10Gamma；
- Y：log10(relative sensitivity)；
- 气泡大小：weakest_right_singular_weight；
- 10°/15°分组；
- boundary_hit 采用描边或标记。

最新正文口径：

- 列归一化条件数约 3.09 / 9.64；
- 15° 最弱方向中 N+Gamma 权重约 81.9%；
- 厚度权重不足 1%；
- 11参数扩展条件数约 1.27e6 / 6.19e4。

## 3. 图替换原则

先保持当前正文 figure 环境、caption、label 不动，仅替换图片资产；替换后重新编译局部稿和总稿检查版面。
