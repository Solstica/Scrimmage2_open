# Compatibility directory

Editable Python code has moved to `shared/code/`, `modules/*/code/`, and
`scripts/`. This file keeps the historical CUMCM gate's `code/` existence
check compatible; do not add new implementations here.

