# Q2 流程图与结果图占位回填

## 当前正文状态

`feature/q2-paper-a` 已按 Q1 的正式化方式重构为：

`问题二的描述与分析 → 总体流程图占位 → 数据预处理与参数设置 → 模型建立 → 模型汇总 → 模型求解 → 结果与分析 → 模型验证与结果可信度`。

冻结主结果仍为：

- `d_10 = 7.8566 μm`；
- `d_15 = 7.6230 μm`；
- 算术平均 `d_SiC = 7.7398 μm`；
- 两角度完全独立拟合，不存在正式 shared/joint fit。

## 1. 总体流程图占位

当前正文已经插入：

`modules/30_q2/figures/editable/origin_templates/reference_png/2.png`

该图来自旧论文手版本，现阶段用于保持整篇论文版式和图文结构完整，但图内仍有过期逻辑，最终必须在可编辑源文件上传后做最小替换。

### 必改项

- 删除/改写任何 `双角度共享参数联合拟合`、`联合拟合` 作为正式主路线的文字；
- 删除用于在“联合拟合/单角度拟合”之间选择的判断菱形；
- 主线改为：`弱吸收波段预处理 → Q1双光束正演模型 → 多初值吸引域筛选 → 10°/15°独立有界最小二乘 → 各自最低残差盆地 → 两厚度算术平均 → 7.7398 μm`；
- 验证框改为 `RMSE、R²、角度差异、最佳/次优盆地残差比较`；
- 不得写“所有初值收敛到同一点”；当前正确结论是“多盆地存在，但最低残差盆地占优”；
- 平均值不是一次新拟合，因此平均框不得附加 RMSE 或 R²。

保持当前正文 label `fig:q2-overall-flow` 不变。

## 2. 正式算法细化图

当前正文同时保留：

`modules/30_q2/figures/q2_algorithm_flow.tex`

这张图是当前模型口径正确的程序化细化图，可继续作为算法层辅助图。最终若论文手新版总体流程图已经足够详细，可在版面压缩阶段决定是否删除该图；现阶段先保留以提高正文完整度。

## 3. 两角度拟合结果图

当前正文使用：

- `q2_10deg_paper_a_fit.png`
- `q2_15deg_paper_a_fit.png`

这两张图由当前冻结求解器生成，数值口径正确，可作为正文占位；最终优先替换为论文手/Origin统一样式的 10°、15°实测--模型拟合图，保持 `fig:q2-paper-a-fit` label 不变。

最终图必须使用当前独立拟合数据，不得重新引用旧 shared/joint fit 数据。

## 4. 多初值盆地图

最终正文建议增加 Q2 图13“多初值局部盆地图”，数据口径见：

`fix/论文手/todo/04_Origin图13_19_23绘制说明.md`

正式结论：

- 10° 次优盆地 RMSE 比最优高约 39.3%；
- 15° 次优盆地 RMSE 比最优高约 11.2%；
- 图中保留全部候选盆地，不删除次优解；
- 图意为“目标函数存在多峰，但正式厚度对应完整精修后的最低残差盆地”。

在 Origin 图尚未完成前，不阻塞正文其他章节推进。

## 验收清单

- [x] 旧论文手 `2.png` 已先插入 Q2 正文占位；
- [x] Q2 正文已正式化并删除 `PAPER_A` 内部标签式叙述；
- [x] 正文明确两个角度独立拟合、算术平均；
- [ ] 最终修改 `2.png` 的联合拟合旧逻辑；
- [ ] Origin 统一样式替换两角度拟合图；
- [ ] Origin 图13完成后插入“模型验证与结果可信度”部分；
- [ ] 最终压缩版面时决定是否保留 `q2_algorithm_flow.tex` 细化图。
