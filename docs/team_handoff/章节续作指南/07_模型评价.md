# 模型评价续作指南

唯一正文源：
```text
modules/50_evaluation/paper/evaluation.tex
```

canonical branch：
```text
feature/evaluation
```

初始化：
```bash
git fetch origin --prune
git switch -c task/paper/evaluation-<topic>-YYYYMMDD origin/feature/evaluation
```

要求：
- 评价必须和当前 Q1/Q2/Q3 最终路线一致；
- 不写旧 7.384039 / 3.308745；
- 优点必须有模型或验证依据；
- 局限不能为了“显得完整”凭空造实验条件；
- 若 Q2/Q3 新增稳定性/误差证据，只有验证通过后才能写入优点。

提交：
```text
docs(evaluation): ...
```

涉及数值时运行：
```bash
python scripts/verify_paper_a_results.py
```
