# Q1 正文对标 PAPER_A 结构审查

## 对标基准

- `PAPER_A`：邱杰等《基于色散模型和最小二乘拟合的外延层厚度测量》。本项目正式结果主要向该文靠拢；其 Q1 采用 Sellmeier--Drude + Fresnel 双光束路线，并按“问题分析—模型建立（光程差/相位差/折射率/反射率）—模型总结”组织。
- `PAPER_B`：路子涵等《基于 Cauchy--Drude 色散效应的外延层厚度红外干涉测量研究》。仅参考叙事、图文组织和分层推导，不采用其 Cauchy 模型、联合拟合和后续厚度结果。

## Q1 当前主要问题（按优先级）

### P0：最终论文语体问题

- [ ] 删除正文中的内部工程词 `PAPER_A`，不能让正式论文暴露内部对标标签。
- [ ] 将“程序接口”“程序实现按同一顺序组织”“合成光谱恢复”等开发语体从核心模型正文移除或改为正式的“模型自洽性检验”。
- [ ] Q1 末句不得暗示第三束强度比是 Q3 的模型选择器；应改为“Q3 通过 Airy 模型与高阶光束量级分析讨论双光束近似的适用范围”。

### P0：核心推导缺口

- [ ] 当前正文从 Snell 定律直接跳到往返相位 `Phi`，缺少题目最核心的几何光程差推导。补入简洁推导：
  - `AC=CB=d/cos gamma`；
  - 同一波阵面比较两束光；
  - 结合 Snell 定律得到 `Delta=2 n_2 d cos gamma`；
  - 再得到 `Phi=2pi Delta/lambda=4pi n_2 d cos gamma/lambda`。
- [ ] 明确说明带符号 Fresnel 振幅系数已经包含界面反射相变，因此不再额外加固定 `pi`；这一点保持当前正确路线，不照抄 PAPER_A 的固定半波损失写法。

### P1：章节结构需向范文/队内规范靠拢

建议 Q1 调整为：

1. `问题一的描述与分析`
2. **立即插入现有 Q1 流程图占位**
3. `模型建立`
   - `光程差与传播相位`
   - `Sellmeier--Drude 折射率模型`
   - `Fresnel 双光束反射率模型`
4. `模型汇总`
5. `模型自洽性检验`（短节）

当前“Sellmeier--Drude折射率的预备工作”和“Sellmeier--Drude--Fresnel双光束测厚模型”层级不够像竞赛正式论文；流程图放在模型汇总之后也不符合“分析→流程图→建模”的阅读顺序。

### P1：Q1 对 Q2 的反向依赖

- [ ] 当前写法“在问题二选取的弱吸收波段内忽略介电函数虚部”使 Q1 的基础模型依赖后续 Q2 的数据选择，叙事顺序不理想。
- [ ] 改为先给出一般 Drude 复介电响应或说明其完整形式；随后说明对 SiC 透明/弱吸收区，Q1 基础双光束模型采用实折射率近似。Q2 再依据数据选择具体弱吸收窗口。
- [ ] 不改变当前 Q1/Q2 frozen 数值路线。

### P1：模型汇总形式不足

- [ ] 当前“模型汇总”仅为一段文字。参考 PAPER_A，改成独立小节，并以一个紧凑的 `aligned/cases` 公式组列出：
  - `lambda=10^4/sigma`；
  - Sellmeier--Drude 折射率；
  - Snell；
  - `Phi`；
  - `rho_s,rho_p`；
  - `R_th=(|rho_s|^2+|rho_p|^2)/2`。
- [ ] 汇总后用 2--3 句解释“输入—中间物理量—最终反射率”的关系，不重复整段推导。

### P2：数学表述可进一步增强

- [ ] `m^*` 固定的理由不要只写“避免与 N 冗余”，应指出 Drude 修正中只出现 `N/m^*` 组合，因此同一光谱无法同时独立识别二者；固定文献有效质量属于结构可辨识性处理。
- [ ] Sellmeier 参数向量不要记作 `boldsymbol s`，避免与 s 偏振混淆；建议统一为 `boldsymbol beta` 或 `boldsymbol theta_S`。
- [ ] 补入核心模型文献引用：薄膜/Fresnel 可引 `bornwolf`，Drude 引 `drude`，对标论文仅作为方案参考可引 `paperA`，正文不要写“以 PAPER_A 为主体”。

## 图形策略

- 当前 `q1_interference_schematic.png` 保留并继续占位。
- 当前 `q1_model_flow.pdf` 即使尚未最终修改，也先移动到“问题一的描述与分析”之后占位，保持全文结构完整。
- 最终流程图文字修正由 `fix/论文手/todo/03_流程图更新清单.md` 与 `06_占位图先插入与Q1图文回填.md` 跟踪。
- `q1_synthetic_recovery.png` 默认不进入正文，若后续需要模型检验图则优先放附录。

## 验收标准

Q1 修改完成后应满足：

1. 不出现 `PAPER_A`、`程序接口` 等内部工程语言；
2. 有“问题分析→流程图→模型建立→模型汇总→模型检验”的完整结构；
3. 有光程差 `Delta=2 n_2 d cos gamma` 的可读推导；
4. 不额外添加固定半波损失，与带符号 Fresnel 系数保持一致；
5. Q1 自身逻辑闭合，不依赖“Q2 已经选择 2500--3300 cm^-1”才能成立；
6. 不改变 Q2 冻结厚度 `7.7398 μm` 与后续正式接口。
