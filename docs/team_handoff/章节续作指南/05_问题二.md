# 问题二续作指南

canonical branch：
```text
feature/q2-paper-a
```

唯一正文源：
```text
modules/30_q2/paper/q2.tex
```

preview：
```text
modules/30_q2/paper/q2_preview.tex
```

正式结果：
```text
output/results/q2_paper_a_results.json
```

当前冻结结果：
- 10°：7.8566 μm
- 15°：7.6230 μm
- 平均：7.7398 μm

正式路线：
**两个角度分别反演，再取算术平均。**

禁止恢复：
- 共享参数双角度联合拟合作为正式主结果；
- 7.384039 μm 旧结果。

初始化：
```bash
git fetch origin --prune
git switch -c task/<role>/q2-<topic>-YYYYMMDD origin/feature/q2-paper-a
```

Origin 模板：
```text
modules/30_q2/figures/editable/origin_templates/
```

其中 `2.png`、01–14 图及数据包暂未接正文，因为部分逻辑/数据仍旧。
**不是被删除。**

更新顺序：
1. 建模手确认每张图正确逻辑；
2. 编程手重算数据包；
3. 论文手用 Origin 原模板替换数据；
4. 输出规范名到 `modules/30_q2/figures/`；
5. preview 验收后再引用。

特别提醒：
当前 Git 尚未确认 Q2 全部 `.opju/.opj` 原工程，若制图人员电脑还有，优先补交，禁止仿造一套新工程替代。

验证：
```bash
python scripts/verify_paper_a_results.py
powershell -ExecutionPolicy Bypass -File ./scripts/build_previews.ps1
```
