# 论文手任务 05：Q2/Q3 最终验证图组取舍与插入位置

流程图源文件尚未回传，本任务先处理不依赖流程图源文件的正文图组。

## Q2

最终正文只建议新增一张 Origin 验证图：**图 13 多初值局部盆地图**。

- 数据：`modules/30_q2/tables/q2_multistart_refined.csv`
- X：`thickness_um`
- Y：`rmse`
- 气泡大小：`support_count_stage1`
- 颜色/点型：10°、15°
- 保留所有 refined basin，不删除次优盆地
- 最优点可加描边/排名标签

建议图注：

> 问题二多初值搜索下的局部吸引盆地及最终残差。两个角度的目标函数均存在干涉级次诱导的多个局部盆地；正式厚度取完整参数精修后 RMSE 最低的盆地，而非由“所有初值收敛”假设确定。

插在 `q2.tex` 的“验证、灵敏度与局限性”多盆地文字之后。

Q2 图 03/04、09、10、12、14 先不进正文；保留模板备用。

## Q3

最终正文只建议新增一张 Origin 验证图：**图 19 Jacobian--SVD 参数可辨识性图**。

- 数据：`modules/40_q3/tables/q3_identifiability_parameters.csv`
- X：`d, n3, log10 N, log10 Gamma_e`
- Y：`log10(relative_sensitivity)`
- 气泡面积：`weakest_right_singular_weight`
- 颜色/点型：10°、15°
- `boundary_hit=True` 使用特殊描边/空心标记

建议图注：

> 问题三四参数 Airy 反演的局部可辨识性。纵轴为尺度化局部灵敏度，气泡面积表示最弱右奇异向量中的参数权重。厚度在两个角度均保持较高灵敏度，而载流子相关参数在部分角度呈现低灵敏度、强耦合与边界活跃现象。

插在 `q3.tex` 的“可靠性与局限性”中 Jacobian--SVD 描述之后。

图 23 多初值盆地图保留为备用/附录候选，默认不进正文，避免与图 19 同时占版面。

## 11 参数扩展

不单独画主图。正文保留条件数对比即可：

- 10°：`3.09 -> 1.27×10^6`
- 15°：`10.11 -> 6.18×10^4`

这部分只用于解释“固定振子背景参数”具有可辨识性依据。
