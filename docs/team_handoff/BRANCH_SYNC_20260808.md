# 论文分支同步说明（2026-08-08）

## 1. 分支架构

当前采用“模块独立、公共层独立、全文只在临时整合分支组合”的结构。各问题分支和章节分支不得把总融合/全文预览分支作为自身父历史。

Canonical branches：

- 摘要：`feature/abstract`
- 问题重述：`feature/restatement`
- 符号说明：`feature/notion-paper-a`
- 模型假设：`feature/assumption-paper-a`
- 问题一：`feature/q1update`
- 问题二：`feature/q2-paper-a`
- 问题三：`feature/q3-paper-a`
- 模型评价：`feature/evaluation`
- 目录/全文入口：`feature/toc`
- 参考文献、附录、AI报告、验证脚本、结果/图注册表等公共层：`feature/paper-common-final`
- `task/paper/final-review-fixes-20260808`：仅保留历史终审快照，不作为任何模块开发基线。

`main`暂不改动。

## 2. Q1--Q3 当前冻结口径

### Q1 — `feature/q1update`

- 正式模型：Sellmeier--Drude--Fresnel双光束模型；
- 弱吸收实折射率口径不变；
- 保留总体流程图、干涉机理图和正演伪代码，Q1伪代码不压缩删除；
- 保留物理极限、单位换算和相位约定检验；
- 条纹间距近似作为Q2/Q3厚度数量级初始化的理论接口。

### Q2 — `feature/q2-paper-a`

- 两角度独立反演，不使用共享参数/联合拟合；
- 拟合区间2500--3300 cm$^{-1}$；
- 90组确定性第一阶段初值，候选去重后保留8个盆地，再释放完整十参数精修；
- 最优厚度盆地在固定Sellmeier参数的第一阶段已出现，释放七个Sellmeier参数后仍保持最低RMSE，作为厚度对附属参数自由化的补充稳健性证据；
- 多初值只用于局部盆地筛查，不证明全局唯一性；
- $d_{10}=7.8566\,\mu$m，$d_{15}=7.6230\,\mu$m，等权平均$d_{SiC}=7.7398\,\mu$m；
- 半极差0.1168 μm只描述角度离散，不称置信区间；
- 表格统一使用`CumcmLightBlue`。

### Q3 — `feature/q3-paper-a`

- 正式模型：双振子--Drude复折射率 + Airy无限多光束模型；
- 两角度独立反演，$d_{10}=3.2474\,\mu$m，$d_{15}=3.1875\,\mu$m，等权平均$d_{Si}=3.2175\,\mu$m；
- 多光束形成条件由$|q_u|\not\approx0$、相干光程条件及空间/几何稳定条件共同描述；
- 厚度物理边界0.1--100 μm与局部初值2.70--3.60 μm的作用已区分：初值只检验由Q1条纹间距关系给出的若干微米数量级附近的局部盆地；
- 双光束只作同参截断诊断，不产生第二套正式厚度；
- $\eta_3$定义为第三束反射光相对于表面反射束的非偏振强度比；
- 0.1%只作工程量级参照，代码和JSON已取消阈值自动选模布尔值；
- 图21采用指数包络去趋势$Q_i$及由原始$\eta_3$计算的$D_\theta$解释，不再按旧dB直线语义解释；
- 15°的$R^2=0.0741$继续披露，多初值/SVD仅作当前模型内部局部稳定性证据；
- 正文主证据为流程图、机理图、图15、图21、图19；图17/18/20/24不得恢复。

详细规则见`docs/team_handoff/Q3_VERSION_SYNC_20260808.md`和`Q3_FIGURE_REVIEW.md`。

## 3. 非Q章节当前口径

### 摘要 — `feature/abstract`

- 冻结结果保持SiC 7.7398 μm、Si 3.2175 μm；
- $\eta_3$统一为第三束相对于表面反射束；
- 0.1%不承担选模含义；
- 保留$\Delta R\rightarrow\Delta d$和Jacobian--SVD作为Q3主要亮点。

### 问题重述 — `feature/restatement`

只重述任务、输入与输出，不提前讲具体算法答案。

### 符号说明 — `feature/notion-paper-a`

已统一复折射率、消光系数、复介电函数、$N$、$\Gamma_e$和$\eta_3$等跨章节符号；$\eta_3$定义与Q3正文/代码一致。

### 模型假设 — `feature/assumption-paper-a`

按照老师“3--6条、关键假设完整、每条说明依据”的规范重写为6条短标题假设：

1. 理想分层结构；
2. 测量状态稳定；
3. 非偏振独立传播；
4. 波段内光学模型适用；
5. 同晶圆厚度一致；
6. 窄波段参数稳定。

每条均给出物理理由，不再把“取算术平均”“只拟合四参数”等求解策略直接冒充假设。

### 模型评价 — `feature/evaluation`

- 加入Q2参数自由化前后的厚度盆地延续性；
- $\eta_3$统一为第三束相对于表面反射束；
- 继续明确15°低$R^2$和模型系统失配限制；
- SVD不被解释为消除了模型偏差。

### 目录/全文入口 — `feature/toc`

训练赛目录保留到subsection级；全文标题覆盖色散双光束和Airy多光束两个阶段。

### 公共层 — `feature/paper-common-final`

- `figure_registry.csv`已同步图21最终去趋势语义；
- `result_registry.csv`已同步Q3描述性SiC回溯结构和新结果文件哈希；
- `verify_paper_a_results.py`不再检查`retain_q2_result`阈值布尔值，而检查数量级参照字段和Q2依赖一致性；
- 当前版本索引已恢复独立branch架构说明；
- 参考文献、附录和AI使用报告维持正式口径。

## 4. 文献来源提醒

Q3精确双振子参数$(11.68,0.29972,11.34965,10.6683,16.2026,0.092613,0.23572)$可在团队采用的参考方案代码中直接核对。提交前不得在缺乏原文证据时把整套参数强行归因于Li 1980；Li 1980可作为硅折射率/色散背景文献，精确参数来源应按实际可核验出处表述。

## 5. Git历史与后续协作

- Q1/Q2/Q3及各章节均在各自canonical branch独立推进；
- 临时全文整合分支不得反向merge到章节分支；
- `main`只在最终全文验收后集成；
- 正式数值以`output/results/*.json`与`work/result_registry.csv`为登记口径；
- Origin正文尺寸：总体流程图1.0 textwidth，双面板0.88，单个同规格小图0.44。
