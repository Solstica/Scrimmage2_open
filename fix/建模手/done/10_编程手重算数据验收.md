# 编程手重算数据验收

状态：已验收 `feature/paper-common-final` 提交 `6620dd8`（“重算数据”）。

## Q2

重算数据保持正式路线不变：10°、15°独立拟合后等权算术平均，不存在共享参数联合拟合。`origin_data/manifest.json` 登记的冻结值仍为：

- 10°：`7.856635820616846 μm`
- 15°：`7.622957415253701 μm`
- 平均：`7.739796617935274 μm`

重算后的图表数据还给出：

- 10°：RMSE=`5.9820245358e-4`，MAPE=`0.267955%`，R²=`0.9551268`；
- 15°：RMSE=`7.0652151562e-4`，MAPE=`0.297776%`，R²=`0.9526453`；
- 多初值最佳/次优残差关系与建模手此前诊断一致：10°次优 RMSE 高约 39.3%，15°高约 11.2%。

因此 Q2 正文已采用 MAPE=0.2680% / 0.2978%，主厚度不变。

## Q3

重算包保持正式 Airy 四参数独立反演与 SiC 回溯路线不变，冻结厚度仍为：

- 10°：`3.2479974169771597 μm`
- 15°：`3.1875196790859226 μm`
- 平均：`3.217758548031541 μm`

新的 Jacobian 细节与此前独立诊断有轻微数值差异，但不改变结论：

- 列归一化 Jacobian 条件数约为 `3.0885`（10°）和 `9.6436`（15°）；
- 15°最弱方向中 `N + Gamma` 权重约 `81.9%`，厚度仍不足 `1%`；
- 11 参数前向扩展条件数约 `1.2668e6`（10°）和 `6.1857e4`（15°）。

这种小差异来自弱可辨边界参数存在近等价最优点；后续 Q3 正文化应优先使用本次 `origin_data` 的最新值，并避免把条件数写成具有过高有效数字的“唯一常数”。

## 数据位置

编程手已在 common-final 生成可直接供 Origin 使用的数据：

```text
modules/30_q2/figures/editable/origin_data/
modules/40_q3/figures/editable/origin_data/
```

两目录各含 `manifest.json`，记录冻结值、附件 SHA256、CSV 行列定义和文件 SHA256。跨章节生成脚本位于：

```text
scripts/export_origin_data.py
```

这些数据视为当前图表重算输入；旧 `reference_png` 只作版式/占位参考，不再作为数值真值。
