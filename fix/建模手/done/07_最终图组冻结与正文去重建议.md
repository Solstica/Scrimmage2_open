# 建模手任务 07：最终图组冻结与正文去重建议

> 目的：在流程图源文件尚未回传前，先冻结 Q2/Q3 最终正文图组，避免把已有 Origin 模板全部塞进 27 页正文。

## 一、总体原则

当前论文已经有正式拟合曲线、残差、厚度表格和 SiC 回溯图。后续新增图必须提供“正文尚未回答的新问题”，否则只保留为 Origin 备用模板或附录候选。

因此最终正文不追求“每个统计指标一张图”，而只保留能支撑核心模型结论的验证图。

---

## 二、Q2 最终正文图组

### 正文保留

1. 现有 `q2_10deg_paper_a_fit.png` + `q2_15deg_paper_a_fit.png` 双角度拟合/残差图。
2. 新 Origin 图 13：多初值局部盆地图。

### 不建议再单独进正文

- 图 03/04 实测--模型一致性散点：与现有“拟合曲线+残差”信息高度重复；留作备用。
- 图 09 厚度柱状图：正文已有公式和厚度表格，重复展示 7.8566/7.6230/7.7398 的信息增益低。
- 图 10 RMSE、图 12 R²：正文已经给出数值；除非论文手需要视觉填充，否则不单独占图位。
- 图 14 残差 ECDF：可作为附录/备用误差诊断。

### 图 13 最终定义

- X：完整精修后厚度 `thickness_um`；
- Y：完整精修 RMSE；
- 颜色/点型：10°、15°；
- 气泡大小：该 refined basin 对应的 stage-I 支持数；
- 必须显示全部 refined basin，不能删除次优点；
- 最优盆地可做描边或标签。

图注建议：

> **图X 问题二多初值搜索下的局部吸引盆地及最终残差。** 两个角度的目标函数均存在干涉级次诱导的多个局部盆地；正式厚度取完整参数精修后 RMSE 最低的盆地，而非由“所有初值收敛”假设确定。

正文插入位置：`\subsection{验证、灵敏度与局限性}` 中多盆地文字之后。

---

## 三、Q3 最终正文图组

### 现有正文继续保留

1. 多光束干涉光路示意图；
2. 10°/15° Airy 正式拟合与残差；
3. 第三束光强比 + PAPER_A 厚度对照组合图；
4. 同一冻结参数下 double/Airy 辅助比较；
5. SiC 第三束回溯图。

### 新增正文优先图

仅新增 **图 19：Jacobian--SVD 参数可辨识性图**。

### 图 23 的最终处理

Q3 多初值图 23 保留数据和 Origin 模板，但默认不进正文。

原因：

- 当前“可靠性与局限性”已经用文字给出 6 组确定性初值的盆地分布；
- 图 19 能回答更关键的“为什么厚度可信而 N、Gamma_e 不应作材料参数解释”；
- 同时加入 19 和 23 会让 Q3 验证图过密，且不利于 27 页正文控制。

只有在最终排版仍有版面，或评审侧特别强调多初值稳定性时，才启用图 23。

### 图 19 最终定义

每个角度 4 个参数：

- X：`d, n3, log10 N, log10 Gamma_e`；
- Y：`log10(relative_sensitivity)`；
- 气泡面积：`weakest_right_singular_weight`；
- 颜色/点型：10°、15°；
- 触边参数使用特殊描边或空心标记。

这张图应同时表达：

1. 厚度局部相对灵敏度最高；
2. 10° 下 Gamma_e 几乎不敏感；
3. 15° 下 N、Gamma_e 均极弱；
4. 15° 最弱奇异方向中 N+Gamma_e 权重约 81.97%，厚度约 0.82%；
5. 边界命中与弱可辨识方向一致。

图注建议：

> **图X 问题三四参数 Airy 反演的局部可辨识性。** 纵轴为尺度化局部灵敏度，气泡面积表示最弱右奇异向量中的参数权重。厚度在两个角度均保持较高灵敏度，而载流子相关参数在部分角度呈现低灵敏度、强耦合与边界活跃现象。

正文插入位置：`\subsection{可靠性与局限性}` 中 Jacobian--SVD 文字之后。

---

## 四、Q3 11 参数扩展不单独画主图

11 参数扩展 Jacobian 的主要作用是支撑“固定振子背景参数”的模型选择，不需要再占一张正文图。

正文只报告两个对比：

- 10°：列归一化条件数 `3.09 -> 1.27×10^6`；
- 15°：列归一化条件数 `10.11 -> 6.18×10^4`。

如需补充，可在图 19 图注或旁边的小表中给出；不建议做第二张奇异谱图。

---

## 五、最终图组结论

### Q2

正文新增 **1 张**：图 13 多盆地。

### Q3

正文新增 **1 张**：图 19 Jacobian--SVD 可辨识性。

图 23 及 Q2 03/04/09/10/12/14 统一作为备用或附录候选。

这样新增验证图数量最少，但分别回答两章当前最缺的数学问题：

- Q2：正式厚度为何能从多峰目标函数中被选出？
- Q3：为何保留厚度结论、但不解释弱可辨附属参数，且为何固定振子背景？
