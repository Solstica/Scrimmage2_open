# 问题一续作指南

canonical branch：
```text
feature/q1update
```

唯一正文源：
```text
modules/20_q1/paper/q1.tex
```

局部 preview：
```text
modules/20_q1/paper/q1_preview.tex
```

主要资产：
```text
modules/20_q1/code/
modules/20_q1/figures/
modules/20_q1/figures/editable/origin_templates/
output/results/q1_validation.json
shared/code/
```

初始化：
```bash
git fetch origin --prune
git switch -c task/<role>/q1-<topic>-YYYYMMDD origin/feature/q1update
```

当前模型：
- Sellmeier–Drude–Fresnel 双光束；
- 实折射率计算折射角；
- 复折射率用于 Fresnel/传播相关复量；
- 不重复添加半波损失。

流程图：
```text
modules/20_q1/figures/editable/origin_templates/reference_png/q1_flow_reference.png
```
当前只作参考，未接回正文。不是被删。修正术语/逻辑并确认后再导出规范名 `q1_model_flow.png`。

注意：
- 已有 `q1_interference_schematic.png` 为正式概念图；
- 不重新保存与其内容完全相同的 `q1_concept.png`；
- 不在 `q1.tex` 加顶级 `\section`。

验证：
```bash
python -m unittest -v scripts.test_physics
python scripts/verify_paper_a_results.py
powershell -ExecutionPolicy Bypass -File ./scripts/build_previews.ps1
```
