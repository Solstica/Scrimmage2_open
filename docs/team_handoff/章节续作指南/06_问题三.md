# 问题三续作指南

canonical branch：
```text
feature/q3-paper-a
```

唯一正文源：
```text
modules/40_q3/paper/q3.tex
```

preview：
```text
modules/40_q3/paper/q3_preview.tex
```

正式结果：
```text
output/results/q3_paper_a_results.json
```

当前冻结结果：
- 10°：3.2480 μm
- 15°：3.1875 μm
- Si 最终：3.2178 μm
- SiC 回溯：7.7398 μm

正式路线：
**Airy 始终作为正式多光束模型。**

第三束比：
只用于解释高阶反射影响，不作为模型选择阈值。

禁止恢复：
- Si = 3.308745 μm；
- SiC = 7.384039 μm；
- 0.1% 阈值选择双光束/Airy；
- 联合双光束主路线。

初始化：
```bash
git fetch origin --prune
git switch -c task/<role>/q3-<topic>-YYYYMMDD origin/feature/q3-paper-a
```

Origin 工程已保留：
```text
modules/40_q3/figures/editable/origin_templates/projects/
modules/40_q3/figures/editable/origin_templates/reference_png/
modules/40_q3/figures/editable/origin_templates/data/
modules/40_q3/figures/editable/origin_templates/notes/
```

`q3_flow_reference.png` 和其他旧图未接正文，是因为数据/逻辑待刷新，不是被删除。

更新顺序：
模型逻辑确认 → Python 重算 → Excel 更新 → Origin 换数据/标签 → 正式导出 → preview → 正文引用。

验证：
```bash
python scripts/verify_paper_a_results.py
powershell -ExecutionPolicy Bypass -File ./scripts/build_previews.ps1
```
