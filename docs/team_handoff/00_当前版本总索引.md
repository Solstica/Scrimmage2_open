# CUCCM2026 run_02 当前版本总索引（2026-08-08）

> 本文件记录审稿修复阶段的当前入口。最终内容正在 `task/paper/final-review-fixes-20260808` 集成；`main` 仍为受保护基线，未修改。

## 1. 当前集成分支

```text
task/paper/final-review-fixes-20260808
```

该分支以 `feature/q2-paper-a` 为起点，并已经合入：

- `feature/q3-paper-a`：当前 Q3 正文、代码、表格、Origin 工程与参考 PNG；
- `feature/q1update`：当前 Q1 正文、代码、流程图与验证结果；
- `feature/abstract`：摘要；
- `feature/restatement`：问题重述；
- `feature/notion-paper-a`：符号说明；
- `feature/assumption-paper-a`：模型假设；
- `feature/evaluation`：模型评价。

公共层的目录、附录、AI 使用报告、结果/图表登记和验证脚本在本集成分支继续修复。禁止将 `feature/paper`、`feature/paper_liu` 等历史整分支重新合入。

## 2. 当前冻结数值

### Q1
- Sellmeier--Drude--Fresnel 双光束正演；
- 空气折射率 `n_air=1.0003`；
- 半波相位不重复加入；
- `output/results/q1_validation.json` 六项检查通过。

### Q2
- 10°、15°分别独立有界反演；
- `d_10 = 7.8566358206 μm`；
- `d_15 = 7.6229574153 μm`；
- `d_SiC = 7.7397966179 μm`，正文写 `7.7398 μm`；
- 正式拟合区间 `2500--3300 cm^-1`；
- 不使用共享参数联合拟合作为正式路线。

### Q3
- 硅正式正演模型始终为 Airy 多光束模型；
- `d_10 = 3.2474353635 μm`；
- `d_15 = 3.1874752425 μm`；
- `d_Si = 3.2174553030 μm`，正文写 `3.2175 μm`；
- 统一 `n_air=1.0003`；
- 双光束只在同一组 Airy 最优参数下作截断比较，不生成第二套正式厚度；
- SiC 第三束最大光强比约 `4.8518e-7 %`，`0.1%` 仅为工程量级参照，不是 Airy/双光束模型选择阈值；
- SiC 回溯保留 `7.7398 μm`。

## 3. 当前论文入口

全文入口：

```text
paper/main.tex -> paper/paper_template.tex
```

正文唯一编辑源：

```text
modules/00_abstract/paper/abstract.tex
modules/10_restatement/paper/restatement.tex
modules/11_notation/paper/notation.tex
modules/12_assumptions/paper/assumptions.tex
modules/20_q1/paper/q1.tex
modules/30_q2/paper/q2.tex
modules/40_q3/paper/q3.tex
modules/50_evaluation/paper/evaluation.tex
modules/60_references/paper/references.tex
modules/70_appendix/paper/appendix_code.tex
modules/80_ai_report/paper/ai_report.tex
```

当前训练赛稿在摘要后加入目录；附录与 AI 使用报告使用无编号 section 并手动加入目录。

## 4. 正式图组

正文登记文件为 `work/figure_registry.csv`。当前图组：

- Q1：总体流程图、双光束机理图；
- Q2：总体流程图、原始光谱双面板、双角度拟合/残差主图、多初值局部盆地图；
- Q3：总体流程图、多光束机理图、Si 双角度 Airy 拟合图、SiC 第三束回溯图、Jacobian--SVD 可辨识性图。

旧的 Q3 阈值决策图、雷达图、验证偏差瀑布图等不得重新进入正文。

## 5. 验证入口

完成本地拉取后优先运行：

```bash
python -m unittest -v scripts.test_physics
python scripts/verify_paper_a_results.py
python scripts/check_writing_quality.py
```

随后编译：

```powershell
powershell -ExecutionPolicy Bypass -File scripts/build_previews.ps1
powershell -ExecutionPolicy Bypass -File scripts/build_paper.ps1
```

任何验证失败都应先修正文档、登记表或冻结结果链，不得通过放宽门禁绕过错误。
