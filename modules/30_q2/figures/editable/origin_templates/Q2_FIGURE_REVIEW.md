# Q2 01--14 逐图建模审查

适用分支：`feature/q2-paper-a`。本文件供编程手重绘 Origin 图时使用。

正式口径：10°、15°分别独立反演，厚度为 7.8566 μm 与 7.6230 μm，最终算术平均 7.7398 μm；正式拟合窗口为 2500--3300 cm^-1；Q2 存在多个干涉级次局部极小值，不能解释为所有初值收敛到同一点。

## 数据使用优先级

1. 优先使用本分支最新刷新后的 `modules/30_q2/figures/editable/origin_templates/data/` 中 01--14 对应 Excel；
2. 图 13 的数值结构以 `modules/30_q2/tables/q2_multistart_refined.csv` 交叉核验；
3. `old/` 目录只保留旧版对照，不得作为正式绘图数据源；
4. 若 Excel 与正式表格出现冲突，先停止制图并以正式求解器/表格复核，不允许手工改数值“对齐”。

## 逐图结论

01 10°原始光谱：正文保留。标出 2500--3300 cm^-1 拟合窗口，不叠加旧拟合结果。

02 15°原始光谱：正文保留。与 01 合成双面板，坐标和样式完全统一。

03 10°实测--模型一致性：备用/附录。X=实测，Y=当前独立拟合，必须加 y=x；R² 约 0.9551。

04 15°实测--模型一致性：备用/附录。与 03 同口径；R² 约 0.9526。

05 绝对残差散点：备用/附录。X=波数，Y=绝对残差，用于定位误差波段；若正文已有拟合+残差主图则不再重复。

06 残差直方图：不进正文。如留附录，只用有符号残差且两角度共用 bin；不据此宣称正态性。

07 残差箱线图：不进正文。仅可作为附录描述误差分布，不作为厚度不确定度。

08 残差热力图：备用/附录。颜色应表示分箱后的平均有符号残差；不直接解释成材料机理。

09 厚度柱状图：不进正文。三组厚度用表格更清楚；平均值不是第三次拟合。

10 RMSE 柱状图：不进正文。平均厚度没有 RMSE。

11 MAPE 柱状图：不进正文。10°约 0.2680%，15°约 0.2978%；平均厚度没有 MAPE。

12 R² 柱状图：不进正文。10°约 0.9551，15°约 0.9526；平均厚度没有 R²。

13 多初值稳定性散点：必须重构后进正文。建议改名“多初值局部候选盆地与最终残差”。X=thickness_um，Y=rmse，气泡面积=support_count_stage1，颜色/点型区分 10°/15°；保留全部 refined candidates，最佳点单独描边。10°最佳约 (7.856636,5.9820e-4)，次优约 (7.131794,8.3339e-4)；15°最佳约 (7.622957,7.0652e-4)，次优约 (6.921774,7.8594e-4)。图应表达“多峰客观存在、最低残差盆地胜出”，不能表达“所有初值收敛”或“已证明全局唯一”。

14 绝对残差 ECDF：备用/附录优先。X=绝对残差，Y=ECDF，两角度共图；不作无依据的显著性结论。

## 正文最终优先图组

1. 01+02 原始光谱双面板；
2. 当前正式的实测--独立拟合--残差主图；
3. 重构后的图 13 多初值局部盆地图。

其余图保留为备用/附录，不为了增加图数强行进入正文。
