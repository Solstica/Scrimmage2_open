# 论文手任务 05：Q2/Q3 Origin 图组逐图最终验收

> 建模手已按当前正式模型与编程手重算数据逐图验收。旧 PNG/Origin 版式可以复用，但数据与图意必须以 `origin_data/csv/` 为准；不得从 `old/` 目录回填正式结果。

## 总原则

- Q2 正式路线：10°、15°独立反演，最终算术平均为 `7.7398 μm`；不存在正式 shared/joint fit。
- Q3 正式路线：硅始终采用 Airy 多光束模型；双光束仅作同一冻结参数下的截断比较；`η3` 仅作工程量级解释，不作选模开关。
- 正文图必须回答“模型输出是什么 / 为什么可信 / 近似为什么成立”之一；只有两个数值的柱状图不替代表格。
- 外部 PAPER_A 数值只可用于内部验收，不得以“参考答案比较图”形式进入正式竞赛论文。

## Q2：01–14

| 图号 | 原图意 | 最终处置 | 正式数据源 / 改造要求 |
|---|---|---|---|
| 01 | 10°原始光谱 | **正文保留** | `q2_raw_10deg.csv`；标出或着色 `2500–3300 cm^-1` 正式拟合窗。原始数据不涉及模型路线冲突。 |
| 02 | 15°原始光谱 | **正文保留** | `q2_raw_15deg.csv`；与 01 合成双面板，坐标范围和样式统一。 |
| 03 | 10°实测–模型一致性 | **备用/附录** | `q2_fit_10deg.csv`；若画，X=observed，Y=fitted，加 `y=x`；只表示 10° 独立拟合。 |
| 04 | 15°实测–模型一致性 | **备用/附录** | 同 03；不得出现 joint/shared 字样。 |
| 05 | 绝对残差散点 | **备用/附录** | 用 `q2_fit_*deg.csv` 的 `residual_abs` 对波数；适合定位误差波段，但正文已有拟合残差图时重复。 |
| 06 | 残差直方图 | **不进正文** | `q2_residual_histogram.csv`；若保留，只画 signed residual 共用 bin，不作“正态性证明”。 |
| 07 | 残差箱线图 | **不进正文** | `q2_residual_boxplot.csv`；只有两个角度，信息量低于现有结果表。 |
| 08 | 残差热力图 | **备用/附录** | `q2_residual_heatmap.csv`；两角度 × 波数分箱，颜色=`residual_mean`。若正文需要说明系统残差，可作为 05 的替代，不与 05 同时上。 |
| 09 | 厚度结果柱状图 | **不进正文** | `q2_summary.csv`；只能有 10°、15°、算术平均三项。不得给平均值伪造误差条。现有表格更优。 |
| 10 | RMSE柱状图 | **不进正文** | `q2_summary.csv`；只有两个独立角度，平均结果无 RMSE。 |
| 11 | MAPE柱状图 | **不进正文** | `q2_summary.csv`；正式值约 0.2680%、0.2978%，但两柱图低价值。 |
| 12 | R²柱状图 | **不进正文** | `q2_summary.csv`；正式值约 0.9551、0.9526，表格即可。 |
| 13 | 多初值稳定性散点 | **必须重构后进正文** | **不要再画“稳定收敛到一点”**。用 `q2_basin_refined.csv`：X=`thickness_um`，Y=`rmse`，气泡面积=`support_count_stage1`，颜色/点型=角度，保留全部 8 个精修候选。最佳点加描边，可标 rank。标题改为“多初值局部候选盆地与最终残差”。 |
| 14 | 绝对残差 ECDF | **备用/附录优先** | `q2_residual_ecdf.csv`；X=`residual_abs`，Y=`ecdf`，两角度两条线。只解释误差幅度分布，不作独立同分布/正态性结论。 |

### Q2 正文最终图组

正文建议保留：`01+02` 原始光谱双面板、当前实测–拟合主图、**重构后的 13 多盆地图**。03/04/05/08/14 只作替换候选或附录；06/07/09/10/11/12 不占正文版面。

图 13 必须体现当前真实数值结构：10°最佳盆地 `7.856636 μm, RMSE≈5.982×10^-4`，次优约 `7.131794 μm`；15°最佳 `7.622957 μm, RMSE≈7.065×10^-4`，次优约 `6.921774 μm`。结论是“多峰存在但最低残差盆地占优”，不是“所有初值收敛”。

## Q3：15–24

| 图号 | 原图意 | 最终处置 | 正式数据源 / 改造要求 |
|---|---|---|---|
| 15 | Si双角度拟合带状曲线 | **重绘后进正文，替换当前 Python 拟合图** | `q3_fit_10deg.csv`、`q3_fit_15deg.csv`。实测+Airy 为主；若加入 double，只能标为“同参双光束辅助”，不得表现成另一套独立拟合。残差必须是 Airy residual。两角度双面板。 |
| 16 | Si残差分箱热力图 | **备用/附录；可用于加强15°局限性** | `q3_residual_heatmap.csv`；颜色=signed `residual_mean`。正文已有拟合图时不必叠加；若要强调 15° 系统残差，可用它替代图 23。 |
| 17 | Si厚度一致性棒棒糖 | **正式正文不采用** | `q3_thickness.csv`。若内部验收可画 10°/15°/mean；**必须删除 PAPER_A 3.040 的正式展示**。三个厚度已有表格，图增益有限。 |
| 18 | Si模型综合指标雷达图 | **禁用** | 不建议继续维护。异量纲指标归一化方式任意，且容易把 Airy/双光束误写成“模型竞赛”；无法形成可靠数学结论。 |
| 19 | Si参数可辨识性气泡图 | **必须重构后进正文** | `q3_identifiability_parameters.csv`。X=`d,n3,log10N,log10Gamma_e`；Y=`log10(relative_sensitivity)`；气泡面积=`weakest_right_singular_weight`；颜色/分面=10°/15°；`boundary_hit=True` 特殊描边。使用最新重算值，不抄旧图。 |
| 20 | Si阈值决策阶梯图 | **正式禁用** | 旧“η3≥0.1% 决定双光束/Airy”逻辑错误。`q3_threshold_context.csv` 仅允许内部工程量级背景；如果保留资料图，标题必须改为“η3工程参照敏感性”，且不能出现 yes/no、模型切换、决策箭头。正文已有 21，故默认不画。 |
| 21 | SiC第三束强度比对数曲线 | **正文强保留，Origin替换当前 Python 图** | `q3_sic_backcheck.csv`。Y 建议对数轴；两角度 η3 曲线 + `0.1%` 工程参照线。0.1% 是量级参照，不是 formal model selector。正式 η3 最大量级约 `4.85×10^-7%` 与 `4.66×10^-7%`。 |
| 22 | Si拟合残差箱线图 | **备用/附录** | `q3_residual_boxplot.csv`；可比较两角度误差离散，但不替代拟合曲线与 R²。 |
| 23 | Si多初值局部盆地气泡图 | **附录/备用，不默认进正文** | `q3_multistart_diagnostics.csv`；必须保留全部 6 个初值。X=最终厚度，Y=RMSE(pp)，颜色=角度，可标 seed_id/触边。10°存在 3.2480、3.2243、2.5148 三类结果；15° 5/6 聚到 3.1875，另有 3.3539 较差盆地。图 19 已承担主要数学验证，正文不建议两张都放。 |
| 24 | Si验证偏差瀑布图 | **禁用** | `q3_validation_comparison.csv` 中厚度角差、半极差、相对外部参考偏差量纲/含义不同且不可加和，瀑布图数学语义错误；其中 PAPER_A 比较也不应进入正式竞赛论文。需要时改成文字/表格，不再做瀑布。 |

### Q3 正文最终图组

最终正文建议以：**15 Airy拟合图 + 21 SiC高阶光束回溯图 + 19 Jacobian–SVD可辨识性图** 为 Origin 主图组；现有多光束机理示意图继续保留。16/22/23 作为附录或替换候选；17 不必重画正式图；18/20/24 禁用。

图 19 采用最新 `origin_data`：10°列归一化条件数约 `3.09`，15°约 `9.64`；15°最弱方向中 `log10N` 与 `log10Gamma_e` 权重合计约 `81.9%`，厚度权重不足 `1%`。扩展到 11 参数时列归一化条件数升至约 `1.27×10^6`（10°）和 `6.19×10^4`（15°），该比较用于支持固定振子背景参数，不另画雷达图。

## 图源规则

1. 论文手优先从 `modules/30_q2/figures/editable/origin_data/csv/` 与 `modules/40_q3/figures/editable/origin_data/csv/` 取正式数据。
2. 队友刚刷新到 `origin_templates/data_package` / `origin_templates/data` 的 XLSX 可以直接供 Origin 导入，但若与上述 CSV 有任何差异，以 CSV 与冻结结果为准。
3. `origin_templates/old/` 只保留历史比对，禁止回填正式图。
4. 旧 PNG 只复用版式；图内标题、标注、联合拟合、阈值选模、旧厚度数值必须全部清理。
5. 新图导出后先替换正文占位资产，再将对应 todo 项移入 done。