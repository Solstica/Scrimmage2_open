# Q3 模型变更后的整合层待办

状态：`PENDING_INTEGRATOR`。Q3 功能分支不得直接覆盖整合者负责的最终摘要和模型评价；合入主线时必须按 `work/abstract_slots.yaml` 的冻结事实完成下列替换，然后重新构建 PDF。

## 摘要 Q3 段

删除旧事实：`Neumann--Airy`、有限阶收敛、AIC、`3.3960 μm`、`RMSE降低70.47%`、Si 必须采用多光束。

建议替换为：

> 针对问题三，建立第三束光强判据驱动的双光束—Airy条件反演模型。在完整波段计算第三束相对表面反射光强比，以0.1%阈值控制模型分支，并采用多启动有界最小二乘反演。Si最大第三束光强比为0.00292%，故选择双光束模型；Si外延层厚度为3.3087 μm，两角度相对差为3.37%，SiC保留前问7.3840 μm结果。

关键词中的 `Neumann--Airy多光束模型` 改为 `第三束光强条件反演模型`。

## 模型总结与评价

- 全文总结：Q3 改为第三束光强比与0.1%阈值条件分支；Si厚度改为 `3.3087 μm`；Si 与 SiC 均选择双光束。
- 优点第4条：删除 `rho、AIC、有限阶收敛`；写明完整波段第三束光强比和 `0.05%–0.20%` 阈值敏感性。
- 局限：增加“载流子浓度与碰撞波数命中边界，不能作材料参数解释”。

## 验收

整合后全文搜索不得在活动文件中出现：`3.3960`、`70.47`、`Neumann--Airy`、`有限阶收敛`、`Si 必须采用多光束`。归档目录 `work/archive/stale_q3_neumann_airy/` 除外。
