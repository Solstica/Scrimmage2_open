# 问题重述续作指南

唯一正文源：
```text
modules/10_restatement/paper/restatement.tex
```

canonical branch：
```text
feature/restatement
```

初始化：
```bash
git fetch origin --prune
git switch -c task/paper/restatement-<topic>-YYYYMMDD origin/feature/restatement
```

规则：
- 只重述赛题任务，不提前写求解结果；
- 不新建顶级 `\section`；
- 不把背景材料扩写成综述；
- 不复制到总 `document.tex`。

提交：
```text
docs(restatement): ...
```
