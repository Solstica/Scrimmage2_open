# document 资产待迁移清单

状态：`FROZEN_BY_OWNER`  
原因：队友正在调整合稿，本轮不移动、不改写、不删除。

## 冻结文件

- `modules/document.tex`
- `modules/document.pdf`
- `output/results/document.tex`
- `output/results/document.pdf`
- `output/图/*.png`
- `output/图/*.opju`

## 解除冻结后的目标

| 当前资产 | 目标位置/动作 |
|---|---|
| 跨问 `document.tex` | 按问拆入模块，或移入 `work/drafts/` 作为非正式合稿 |
| 跨问 `document.pdf` | 移入 `work/drafts/`，不得作为正式交付 PDF |
| `output/results/document.*` | 从结果目录移出；结果目录只保留机器可读正式结果 |
| 7张 Q2 PNG | 用冻结结果重算后放入 `modules/30_q2/figures/` |
| 7个 OPJU | 放入 `modules/30_q2/figures/editable/` |

注意：当前七张图源自 7.7398 μm 陈旧路线，不能直接成为正式图。
