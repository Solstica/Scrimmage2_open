# Q1 正文对标 PAPER_A 结构审查（已完成）

## 对标基准

- `PAPER_A`：邱杰等《基于色散模型和最小二乘拟合的外延层厚度测量》。Q1 主要借鉴其“问题分析—模型建立—模型总结”的竞赛论文骨架。
- `PAPER_B`：路子涵等《基于 Cauchy--Drude 色散效应的外延层厚度红外干涉测量研究》。仅参考分层叙事，不采用其 Cauchy、联合拟合和后续结果。

## 本轮已完成

- [x] 删除正文中的内部工程词 `PAPER_A`、`程序接口` 等。
- [x] 将 Q1 结构重排为“问题描述与分析 → 流程图占位 → 模型建立 → 模型汇总 → 模型自洽性检验”。
- [x] 补入几何光程差推导，得到 `Delta=2 n_2 d cos gamma`，再由此得到传播相位。
- [x] 保留带符号 Fresnel 系数处理反射相变，不照抄范文的固定半波损失 `+pi`。
- [x] 消除 Q1 对 Q2 的反向依赖：Q1 只说明透明/弱吸收区采用实部近似，具体 2500--3300 cm^-1 窗口留给 Q2 数据分析。
- [x] 将 `m^*` 固定解释为 `N/m^*` 组合导致的结构可辨识性处理。
- [x] Sellmeier 参数向量由 `boldsymbol s` 改为 `boldsymbol beta`，避免与 s 偏振混淆。
- [x] 新增独立“模型汇总”小节，用核心方程组统一收束波长、折射率、Snell、光程差、相位、双光束振幅和非偏振反射率。
- [x] 将“程序一致性检验”改为正式论文语体的“模型自洽性检验”。
- [x] Q1 末尾不再把第三束光强比写成 Q3 的模型选择器，而改为 Airy 多光束模型与高阶光束量级分析。

## 验证

本地 `q1_preview.tex` 已通过 XeLaTeX 编译；同时通过：

- `python scripts/check_writing_quality.py`
- `python -m unittest -v scripts.test_physics`

本轮没有改变 Q1/Q2 的正式求解器和冻结数值。

## 仍由论文手 todo 跟踪

- `fix/论文手/todo/03_流程图更新清单.md`
- `fix/论文手/todo/06_占位图先插入与Q1图文回填.md`

现有 `q1_model_flow.pdf` 继续作为正文占位图，待可编辑源文件上传后只做最小文字/框线修正，保持 `fig:q1-flow` 不变。
