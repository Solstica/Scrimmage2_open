# Q1--Q3 横向一致性与摘要评价同步

## 结论

三问正式正文已完成第一轮横向口径统一：

- Q1：Sellmeier--Drude--Fresnel 双光束正演；弱吸收时使用实折射率；带符号 Fresnel 系数承担界面反射相变，不额外固定加 $\pi$。
- Q2：$2500$--$3300\,\mathrm{cm}^{-1}$；$10^\circ$、$15^\circ$完全独立的有界多初值反演；正式厚度分别为 $7.8566$、$7.6230\,\mu\mathrm m$，算术平均 $7.7398\,\mu\mathrm m$；不构造共享参数联合拟合。
- Q3：硅使用双振子--Drude 复折射率与 Airy 多光束正式模型；两个角度独立四参数拟合后平均为 $3.2178\,\mu\mathrm m$；第三束光强比仅作量级解释和 SiC 回溯，不作为 Airy/双光束选模器。
- Q3 可辨识性：Jacobian--SVD 用于反演后的局部诊断，不回写厚度；最新重算下列归一化条件数约为 $3.09$ 与 $9.64$，15°最弱方向仍主要来自载流子浓度和碰撞频率耦合。

## 已同步章节

1. `feature/abstract`：删除 PAPER_A、诚实重算、错误零厚度检验等内部/过时语言，改为三问正式模型与结果摘要，并加入 Jacobian--SVD 亮点。
2. `feature/evaluation`：删除结果哈希、冻结值、与范文贴数等开发语言，按物理一致性、多初值反演、可辨识性约束和跨问回溯四点重写模型优点与局限。
3. `feature/assumption-paper-a` 当前 5 条假设与三问正式模型一致，无需改动。
4. `feature/notion-paper-a` 公共符号表继续只保留跨问共用符号，其余拟合参数在首次出现处定义。

## 待后续全文集成时再查

- 流程图与旧 Origin 图仍是占位资产，按论文手 todo 替换，不据旧图反改正文。
- 全文只允许一个参考文献模块，由 `paper/main.tex -> sections/91_references.tex -> modules/60_references/paper/references.tex` 统一输出。
- 完整合并后重新检查图号、表号、交叉引用、浮动体位置、页数与参考文献引用顺序。
