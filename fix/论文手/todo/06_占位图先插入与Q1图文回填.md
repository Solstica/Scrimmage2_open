# 占位图先插入与Q1图文回填

## 统一策略

从本轮开始，正文完整度优先于“所有图都已最终定稿”。只要图在逻辑上属于正文，应先插入现有旧版图、当前 Python 预览图或其他可识别占位图，使整篇论文的章节结构、图表编号和图文关系完整；随后在本目录保留明确替换任务。

占位图允许暂时不是最终 Origin / 人工流程图，但必须满足：

- 图意与当前正文位置一致；
- 图注使用当前模型口径，不沿用已废弃结论；
- 若图本身包含旧模型文字，应在本待办中明确记录“图内文字仍旧，最终必须替换”；
- 占位图不得被误认为最终验收图，最终源文件上传后再替换；
- 替换图时尽量保持 label 与正文引用不变，避免重新调整全文编号。

## Q1 当前图

### 1. 双光束干涉光路图

当前正文已经插入：

`modules/20_q1/figures/q1_interference_schematic.png`

处理：可继续作为正文正式占位。最终检查标注中空气、外延层、衬底、两束光、入射角/折射角、厚度符号是否与正文统一。

### 2. Q1 模型流程图

当前正文已经插入：

`modules/20_q1/figures/q1_model_flow.pdf`

处理：立即保留在正文，不等待论文手源文件；但它目前只作为占位流程图。Q1 正文已经把该图移动到“问题一的描述与分析”之后，当前图内仍含旧字样（包括 `PAPER_A`），因此最终源文件上传后必须按 `03_流程图更新清单.md` 的 Q1 最小替换方案修正图内文字，不改变现有 label。

### 3. 合成恢复图

`modules/20_q1/figures/q1_synthetic_recovery.png`

当前不建议进入正文。Q1 是解析模型建立题，正文已有光路图和流程图；若后续版面允许，可放模型检验/附录，不作为核心结果图。

## Q1 当前正文状态

`feature/q1update` 已完成第一轮结构回填：

- “问题描述与分析 → 流程图占位 → 模型建立 → 模型汇总 → 模型自洽性检验”结构已形成；
- 已补入 `Delta=2 n_2 d cos gamma` 的几何光程差推导；
- 正文已删除 `PAPER_A` 等内部工程语体；
- 固定半波损失没有重新引入，反射相变继续由带符号 Fresnel 系数承担。

## Q1 图文回填待办

- [ ] 流程图最终替换：删除/更正图内旧术语，使其与“Sellmeier--Drude--Fresnel 双光束模型”一致，尤其删除图内 `PAPER_A`。
- [ ] Q1 光路图最终检查：图中符号与正文统一为 `n_1,n_2,n_3,i,\gamma,d`。
- [x] 流程图位置已移动到“问题一的描述与分析”之后。
- [ ] 最终图源文件上传后替换占位文件，但保持 `fig:q1-flow`、`fig:q1-schematic` 的 label 不变。
- [ ] 若后续版面允许，再决定 `q1_synthetic_recovery.png` 是否进入附录；默认正文不增加第三张 Q1 图。
